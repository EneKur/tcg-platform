# Session Log — 2026-05-24

## Branch
`2026-05-24-cleanup-steel` → merged to `main`

## What was done

### Cleanup
- Removed all Steel.dev related code (`steel-sdk`, `steel_session`, `auth/`)
- Purged `.DS_Store`, `.dg/`, `.tmp_dagster_home_*/`, `.scrape_inspect.py` from git history
- Rewrote `.gitignore` to block dagster temp dirs and macOS metadata
- Deleted stale remote branches: `2026-05-24-cleanup-steel`, `2026-05-24-M1-T1`, `origin/2026-05-22-M2-T3`
- Rewrote README with current project state, added `.env.example`

### Bronze Layer Implementation
- `CurrencyRatesDB` resource — SQLite store for EUR→GBP exchange rates
- `exchange_rates_asset.py` — Dagster asset, backfills from Frankfurter.app (2023-06-01 → today)
- `extract_item_image_url` in `ebay.py` — regex extraction of eBay image URLs
- `scraping/ebay_image.py` — downloads card images and uploads to MinIO (`sold_images/{region}/{item_id}.jpg`)
- `PriceRecord` model updated with `image_url` and `local_image_path` fields
- `fact_events` table ALTER'd to add `image_url TEXT`, `local_image_path TEXT`
- `bronze_ebay_sqlite_writer` updated to write image fields to SQLite
- `ebay_de_sold_listings` + `ebay_uk_sold_listings` updated with image scraping + MinIO saving
- `CURRENCY_RATES_DB` env var wired into `definitions.py` via `currency_rates_db` resource

### Bug Fixes
- `required_resource_keys={"currency_rates_db"}` added to `exchange_rates` asset (was missing → `DagsterUnknownResourceError`)
- `setup_for_execution` fixed in `CurrencyRatesDB` (was duplicating method, second had `os.makedirs(None)` causing `TypeError`)
- `CURRENCY_RATES_DB` resource now raises clear error if env var not set

### Git Cycle
- All Rules 13–19 defined and added to `AGENTS.md`

## What remains
- Bronze DE/UK scraping has never been run live (Zyte API working, eBay anti-bot workaround in place)
- Silver/Gold layers not started
- US pipeline data (~1,022 rows) has dirty card_ids — left as-is, not active focus

## Blockers
- eBay anti-bot still an issue for datacenter IPs — Zyte is the workaround

## Commits on branch (9 total)
- `0419460` feat: add CurrencyRatesDB resource
- `e5cdb1a` feat: add exchange_rates dagster asset
- `6a23a7e` feat: add extract_item_image_url to ebay scraper
- `f86b424` feat: add ebay_image module for MinIO image upload
- `49dd2c0` feat: add image_url and local_image_path columns to fact_events
- `0e350be` feat: add image fields to PriceRecord and writer
- `357bbf1` feat: add image saving to DE and UK sold listings assets
- `36f4ff1` fix: add required_resource_keys for currency_rates_db
- `f969887` fix: ensure currency_rates_db raises clear error if CURRENCY_RATES_DB env var missing

## Merged to main
- `main` at `ed61935` after merge + README rewrite + .env.example + .scrape_inspect.py removal