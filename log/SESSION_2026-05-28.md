# Session Log — 2026-05-28

## Branch
`2026-05-27-sold-data-parquet`

## What was done

### Silver Layer (M7) — Complete
LakeSail (pysail) + PySpark 4.1 via Spark Connect running. All silver transforms green.

**Files created/modified:**
- `src/tcg_platform/defs/silver_transform.py` — `_run_silver_transform()`, `_normalize_extracted_id()`, `_is_complete_card_id()`
- `src/tcg_platform/definitions.py` — `silver_de_pipeline`, `silver_uk_pipeline`, `silver_eu_pipeline` jobs
- `scripts/query_silver.py` — DuckDB + MinIO S3 query tool
- `src/tcg_platform/scraping/ebay.py` — `parse_ebay_item_page()` captures `title`; `_normalize_card_id()` expanded to 100 chars
- `src/tcg_platform/scraping/models.py` — `PriceRecord` now includes `title` field
- `src/tcg_platform/serialization/card_parquet.py` — `price_records_to_parquet` includes `title`
- `src/tcg_platform/defs/bronze_sold_data_parquet.py` — added `title` field to `_BRONZE_SCHEMA`

**Key fixes:**
- PyArrow `from_pydict` → `from_pandas` for DataFrame→Parquet conversion (correctly handles dtypes/NA)
- `MinioClientResource.list_objects()` hardcodes recursive=True, removed errant kwarg
- `minio_client` only in `required_resource_keys`, not function param (Dagster treated double-declaration as upstream asset dependency)
- Spark Connect doesn't support `broadcast()` → Python closure captures card_ids as set instead

**Card normalization:**
- 5-digit IDs: `ST13003` → `ST13-003`, `OP07029` → `OP07-029`
- 4-digit IDs: `OP15078` → `ST15-078`
- P promo set: `P043` → `P-043`
- PRB: `PRB01001` → format correct but quarantined (cardlist uses `PRB01-001`)
- Pure set codes (`ST10`, `OP01`) → quarantined (no card number)
- P cards normalized and checked against cardlist (only P-014 through P-105 in collection → most quarantine correctly)

**Verified data (after backfill + silver run):**
| Region | Valid | Quarantine |
|--------|-------|------------|
| UK | 694 | 513 |
| DE | 408 | 335 |

P cards (`P-043`, `P-066`, `P-070`, `P-080`, `P-102` etc.) are in bronze but quarantine correctly — not in cardlist collection yet.

## What remains
- M7-T2: Run silver assets live and verify quarantine ratio
- M7-T3: M7 milestone log
- M8: Gold layer — aggregations, price analytics parquet exports