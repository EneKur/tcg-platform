# M1-T1 Log: Add Steel.dev Dependencies

**Date:** 2025-05-13  
**Status:** ✅ Complete

## Summary

Added Steel.dev SDK and Playwright browser automation dependencies to `pyproject.toml`, added `auth/` to `.gitignore`, verified `uv sync` succeeds.

## Notes

- `puppeteer-core` is Node.js only — not added. Playwright Python is the correct library for this Python project.
- `steel-sdk` latest on PyPI is `0.17.0` — set constraint to `>=0.17.0` (spec originally said `>=1.0.0` but that version doesn't exist yet).
- Steel CLI also installed system-wide via `curl -fsS https://setup.steel.dev | sh`

## Files Changed

- `pyproject.toml` — added `steel-sdk>=0.17.0`, `playwright>=1.40.0`
- `.gitignore` — added `auth/` entry
- Commit: `73ef42d`