# Session 2026-06-27 (M9-T2 replay-bronze-from-raw)

## Branch
`2026-06-27-m9-t2-replay-bronze-from-raw`

## Goal

Build `replay_bronze_from_raw_job` — a Dagster job that closes the
150-row raw-no-bronze gap (89 DE + 61 UK) and enables parser-bug
replays without re-paying Zyte API costs.

## Done

### Spec
- `docs/superpowers/specs/2026-06-27-replay-bronze-from-raw-design.md`

### Plan
- `docs/superpowers/plans/2026-06-27-replay-bronze-from-raw.md`

### Implementation (12 tasks, 13 commits)

**Helper** — `src/tcg_platform/serialization/bronze_writer.py`
- New pure `transform_one_item` helper, mode-aware (`fill` / `overwrite`)
- `_is_proxy_title` filter, `parquet_write_failed` / `sqlite_write_failed` counters
- 11 unit tests in `tests/serialization/test_bronze_writer.py`

**Refactor** — `src/tcg_platform/defs/transform_bronze.py`
- `_transform_region` now delegates per-item work to `transform_one_item` in `fill` mode
- Behavior preserved (all 5 existing tests pass)

**New assets** — `src/tcg_platform/defs/replay_bronze_from_raw.py`
- `replay_bronze_from_raw_de` + `_uk` assets, parallel in one job
- `_enumerate_raw_keys`, `_read_html`, `_read_image_or_none` private helpers
- 6 asset-level tests in `tests/scraping/test_replay_bronze_from_raw.py`

**Job registration** — `src/tcg_platform/definitions.py`
- Added `replay_bronze_from_raw_job` to imports + `jobs=[...]` list

**PROD.md**
- New M9-T2 checkbox with spec reference

### Spec-compliance gap caught + fixed mid-task

Task 4 review surfaced that the initial implementation called
`INSERT OR IGNORE` in overwrite mode regardless of prior-row
existence — violating the spec's "SQLite row untouched" contract.
`prior_row_existed` flag added at the `stat_object` site; SQLite
block gated behind `not (mode == "overwrite" and prior_row_existed)`.
Test renamed to match the spec language. Fix commit `f5476c6`.

### Code review fixes (Task 2)

- Dropped dead `lower = region.lower()` variable (helper takes pre-computed `image_path`)
- Clarified in docstring that `image_path` is caller-supplied
- Pinned `card_id` column index in the new test (catches positional swaps)
- Fix commit `c169013`

### Verification (Rule 17)
- `pytest tests/ -v` — **212 passed** in 10.16s (was 195 at session start; +17 net: +11 helper tests, +6 asset tests)
- `python -c "from tcg_platform.definitions import defs; defs.load_fn()"` — **OK**
- `python -c "...; resolved.get_job_def('replay_bronze_from_raw_job')"` — **OK** (Dagster 1.13 deprecation warning about `resolve_job_def` is informational)
- `git status --porcelain` — clean

### Code review notes

- **Task 4 review** found a real spec violation (overwrite mode SQLite gate). Fixed in commit `f5476c6` with a follow-up review confirming the test pins the spec-correct behavior.
- **Task 2 review** found a dead variable, a missing docstring clarification, and untested column-order. All fixed in commit `c169013`.
- **Task 10 review** flagged that the spec's `dg.build_op_context` API was wrong — direct invocation uses `dg.materialize_to_memory` or asset-key construction. Test adapted; production code unchanged.

## Outstanding (post-this-PR)

- First production use of the job — operator's call.
  Expected (per inventory measured at session start):
  - DE: `mode=fill` writes 89 new bronze parquets + 89 SQLite rows; skips 76
  - UK: `mode=fill` writes 61 new bronze parquets + 61 SQLite rows; skips 386
  - After run: `raw == bronze` per region (raw count == bronze count)
- 29 UK bronze-without-raw rows — cannot be replayed (raw is gone).
- M5-T2 (Dagster schedules) — still deferred.
- `silver_eu_orchestrator` parallelization — separate task.
- DE/UK arbitrage backtest — separate task.

## Blockers
None.

## Notes

- The plan listed "~21 net new tests (195 → ~216)"; actual is +17 (195 → 212). Some planned tests were consolidated; no tests were skipped.
- The plan called for direct asset invocation `replay_bronze_from_raw_de(context=ctx, config={"mode": "fill"})`; Dagster's runtime API uses `dg.materialize_to_memory` instead. Tests adapted; production code is unchanged.