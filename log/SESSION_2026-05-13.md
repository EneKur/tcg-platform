# Session Log — 2026-05-13 (Evening)

## Session Summary

**Started:** ~18:30
**Ended:** ~20:40

## What Was Done

### 1. Steel Browser Session Re-evaluation
- Confirmed Steel browser sessions are **broken** in this environment:
  - Local container: Chrome fingerprint generation fails (entropy issue with Podman on macOS)
  - Cloud WebSocket: 502 Bad Gateway
  - `--privileged` flag on container **did not fix it**
- Decision: Use **Playwright** as primary for JS-heavy sites, Steel `scrape()` for static HTML

### 2. Limitless TCG Scraper (M2-T2) — ✅ Complete
- URL: `https://onepiece.limitlesstcg.com/cards/`
- Playwright + BeautifulSoup approach (no Steel browser session needed)
- Returns `list[CardRecord]` + `list[PriceRecord]` with USD/EUR prices
- Tested successfully: 3 cards from OP15 parsed correctly
- `scrape_limitless_op()` — main entry point, iterates all 49 sets

### 3. Playwright Installation
- `pip install playwright --break-system-packages`
- `playwright install chromium`
- BS4 and lxml installed

### 4. Dagster Assets — Partial (M5-T1)
- `dg dev` starts successfully, loads assets
- Scaffolded `limitless_op_cards` asset via `dg scaffold defs dagster.asset`
- Asset is registered but doesn't persist data yet (no MinIO/SQLite wiring)

## Current Project State

| Milestone | Status |
|-----------|--------|
| M1 (Steel resources) | ✅ Complete |
| M2-T1 (PriceCharting) | ✅ Complete |
| M2-T2 (Limitless TCG) | ✅ Complete |
| M2-T3 (Images) | ❌ Not started |
| M3 (MinIO) | ❌ Not started |
| M4 (SQLite) | ❌ Not started |
| M5 (Dagster defs) | ⚠️ Partial (1 asset, no persistence) |
| M6 (eBay) | ❌ BLOCKED (Steel sessions broken) |

## Key Files Changed

- `src/tcg_platform/scraping/limitlesstcg.py` (new)
- `src/tcg_platform/defs/limitless_op_cards.py` (new)
- `log/M2-T2.md` (new)
- `PROD.md` (updated milestones, dependencies)

## Next Session Start

1. Resume M5-T1: Wire MinIO/SQLite persistence into assets
2. Build M2-T3: Image download utilities from Limitless TCG
3. Build M3-T1, M3-T2: MinIO integration
4. Build M4-T1, M4-T2: SQLite integration
5. Revisit M6-T1/T2: eBay scraping — try Playwright approach (may work without Steel)

## Blockers

- **Steel browser sessions** not usable in this environment (Podman + macOS entropy issue)
- **MinIO/SQLite** not yet built — data scraped but not persisted via Dagster
- **eBay** remains blocked until browser session issue resolved or Playwright fallback implemented